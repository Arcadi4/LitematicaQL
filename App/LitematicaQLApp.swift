import SwiftUI

@main
struct LitematicaQLApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .defaultSize(width: 900, height: 640)
        .windowToolbarStyle(.unifiedCompact)
    }
}
